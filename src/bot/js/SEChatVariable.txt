

CHAT
	RoomUsers:
		all()                  //returns a list of all users in the room
			forEach(function)
		allIncludeIncomplete() //returns a list of all pingable users
			forEach(function)
		allPresent()           //returns a list of all current users
			forEach(function)
		createAvatarImage()    // Error
		current()              // returns the current user
		forceUpdate()
		get()
		getIfAvailable()
		initPresent()
		initializeLate()
		monologueSignature()
		on()                    // undefined
		pingableUsersIncludeIncomplete()
		sidebarActivity()
		sidebarLeave()
		trigger()
		update()
		updateAllUserContainersFor()
		updateUserContainer()
		
	Hub:
		endEditing: event
		firstTyping: event
		roomReady: event
	addEventHandlerHook()
	getEventHandlerHooks()
	inputHint:
		show()
	switchMobile()
	CURRENT_ROOM_ID: int
	CURRENT_USER_ID: int
	IS_MOBILE: boolean
	RoomUsers.allPresent().forEach(function(element){console.log(element)})